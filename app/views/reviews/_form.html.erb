<%= form_for([@book, @book.reviews.build]) do |f| %>
<div>
<%= f.label :Rating %>
  <%= f.text_field :rating %>
  </div>
  <div>
  <%= f.label :Comment %>

  <%= f.text_field  :comment %>
  </div>
  <%= f.button :submit %>
<% end %>

